import React from 'react'
import ReactDOM from 'react-dom/client'
import DanangSystemUI from './danang-system-ui'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <DanangSystemUI />
  </React.StrictMode>,
) 